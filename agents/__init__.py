# agents package
# Contains the three reasoning agents: analysis_agent.py, validation_agent.py,
# synthesis_agent.py.  Each file is divided into an INTELLIGENCE section (pure
# functions, no I/O) and an ORCHESTRATION section (entry-point function that
# coordinates calls and emits signals).
